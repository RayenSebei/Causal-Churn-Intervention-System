pandas>=2.1
numpy>=1.26






































































































































































































































































    run_cli()if __name__ == "__main__":        )            f"\n  Expected Churn if Treated: {ex['expected_churn_if_treated']:.2%}"            f"\n  Uplift from Treatment: {ex['uplift']:.2%}"            f"\n  Baseline Churn: {ex['baseline_churn']:.2%}"            f"\n{seg}:"        print(    for seg, ex in results["segment_examples"].items():    print("\n=== Segment Examples (Highest Uplift per Segment) ===")    print(results["segment_counts"])    print("\n=== Segment Counts ===")    )        base_dir / "models" / "baseline_churn_model.joblib",        base_dir / "WA_Fn-UseC_-Telco-Customer-Churn.csv",    results = run_uplift_pipeline(    base_dir = Path.cwd().parent    """Convenience entry point for uplift modeling."""def run_cli() -> None:    return results    results["segment_examples"] = segment_examples            }                "expected_churn_if_treated": float(max(0, baseline_probs_test[top_idx] - cate_learned[top_idx])),                "uplift": float(cate_learned[top_idx]),                "baseline_churn": float(baseline_probs_test[top_idx]),                "index": top_idx,            segment_examples[seg] = {            top_idx = seg_indices[np.argsort(cate_learned[seg_mask])[-1]]            seg_indices = np.where(seg_mask)[0]        if seg_mask.sum() > 0:        seg_mask = segments == seg    for seg in ["Persuadables", "Sure Things", "Lost Causes", "Sleeping Dogs"]:    segment_examples = {}    }        "y_test": y_test,        "X_test": X_test,        "segments": segments,        "cate": cate_learned,        "baseline_probs": baseline_probs_test,        "segment_counts": segment_counts,    results = {    segment_counts = segments.value_counts()    segments = segment_customers_by_cate(X_test, baseline_probs_test, cate_learned)    t_learner_models, cate_learned = fit_t_learner_cate(X_test, treatment_test, outcomes_test, random_state=random_state)    treatment_df = pd.DataFrame({"treatment": treatment_test, "outcome": outcomes_test}, index=X_test.index)    outcomes_test, true_cate = inject_treatment_effect(baseline_probs_test, treatment_test.values, cate_baseline, random_state=random_state)    treatment_test = assign_synthetic_treatment(X_test, random_state=random_state)    cate_baseline = estimate_cate_from_baseline(model, X_test, baseline_probs_test)    baseline_probs_test = model.predict_proba(X_test)[:, 1]    model = joblib.load(baseline_model_path)    )        X, y, test_size=0.2, stratify=y, random_state=random_state    X_train, X_test, y_train, y_test = train_test_split(    y = featured_df["Churn"]    X = featured_df.drop(columns=["Churn"])    featured_df = load_featured_frame(csv_path)    import joblib    """        Dictionary with segment counts, examples, and CATE estimates.    Returns:        random_state: Random seed.        baseline_model_path: Path to saved baseline model.        csv_path: Path to raw Telco CSV.    Args:    """Run the end-to-end uplift modeling pipeline.) -> dict[str, Any]:    random_state: int = 42,    baseline_model_path: str | Path,    csv_path: str | Path,def run_uplift_pipeline(    return pd.Series(segments, index=X.index)    segments[(~high_baseline) & (~low_cate)] = "Sleeping Dogs"    segments[(high_baseline) & (high_cate)] = "Persuadables"    segments[(high_baseline) & (low_cate)] = "Lost Causes"    segments[(~high_baseline) & (low_cate)] = "Sure Things"    high_cate = cate >= cate_high_threshold    low_cate = cate <= cate_low_threshold    high_baseline = baseline_churn >= baseline_threshold    segments = np.empty(len(X), dtype=object)    cate_high_threshold = np.percentile(cate, p_high)    cate_low_threshold = np.percentile(cate, p_low)    baseline_threshold = np.percentile(baseline_churn, 50)    p_low, p_mid, p_high = percentiles    """        Segment assignment (string labels).    Returns:        percentiles: (p_low, p_mid, p_high) for segmentation thresholds.        cate: Heterogeneous treatment effect.        baseline_churn: Baseline churn probability (control arm).        X: Feature matrix.    Args:    """Segment customers into four groups based on baseline churn and CATE.) -> pd.Series:    percentiles: tuple[float, float, float] = (33, 67, 90),    cate: np.ndarray,    baseline_churn: np.ndarray,    X: pd.DataFrame,def segment_customers_by_cate(    return (model_0, model_1), cate        cate = np.zeros(len(X))    else:        cate = pred_0 - pred_1        pred_1 = model_1.predict(X)        pred_0 = model_0.predict(X)        model_1.fit(X[treatment_mask], Y[treatment_mask])        model_0.fit(X[control_mask], Y[control_mask])        model_1 = RandomForestRegressor(n_estimators=50, max_depth=6, random_state=random_state)        model_0 = RandomForestRegressor(n_estimators=50, max_depth=6, random_state=random_state)    if control_mask.sum() > 10 and treatment_mask.sum() > 10:    treatment_mask = T_array == 1    control_mask = T_array == 0    T_array = T.values    """        Tuple of (RegressionRF model, CATE predictions).    Returns:        random_state: Random seed.        Y: Outcomes (churn indicator).        T: Binary treatment indicator.        X: Feature matrix.    Args:    """Fit a T-learner (two separate models: one per arm) to estimate CATE.) -> tuple[Any, np.ndarray]:    random_state: int = 42,    Y: np.ndarray,    T: pd.Series,    X: pd.DataFrame,def fit_t_learner_cate(    return np.clip(cate, 0, 1)    cate = baseline_probs * 0.3 + (1 - baseline_probs) * 0.05    """    Customers with low/stable baseline → low uplift.    High baseline churn → larger potential uplift from treatment.    """Estimate heterogeneous treatment effect magnitude from baseline churn risk.) -> np.ndarray:    baseline_probs: np.ndarray,    X_features: pd.DataFrame,    model: Any,def estimate_cate_from_baseline(    return outcomes, cate    outcomes = np.random.binomial(1, churn_factual)            churn_factual[i] = np.maximum(0, churn_baseline[i] - cate[i])        if treatment[i] == 1:    for i in range(len(churn_baseline)):    churn_factual = churn_baseline.copy()    np.random.seed(random_state)    """        Tuple of (outcomes, true_cate).    Returns:        random_state: Random seed.        cate: Heterogeneous treatment effect (reduction in churn risk if treated).        treatment: Binary treatment indicator.        churn_baseline: Baseline churn probability (control arm).    Args:    """Generate outcomes with injected heterogeneous treatment effects.) -> tuple[np.ndarray, np.ndarray]:    random_state: int = 42,    cate: np.ndarray,    treatment: np.ndarray,    churn_baseline: np.ndarray,def inject_treatment_effect(    return pd.Series(treatment, index=df.index)    treatment = np.random.binomial(1, treatment_prob)    )        treatment_prob_month_to_month,        treatment_prob[month_to_month_mask],    treatment_prob[month_to_month_mask] = np.maximum(    month_to_month_mask = df["Contract"] == "Month-to-month"    treatment_prob[high_charge_mask] = treatment_prob_high_charge    high_charge_mask = df["MonthlyCharges"] > df["MonthlyCharges"].median()    treatment_prob = np.ones(n) * treatment_prob_baseline    n = len(df)    np.random.seed(random_state)    """        Binary treatment indicator series (0=control, 1=treatment/discount offered).    Returns:        random_state: Random seed.        treatment_prob_baseline: P(treatment | baseline).        treatment_prob_month_to_month: P(treatment | Contract == Month-to-month).        treatment_prob_high_charge: P(treatment | MonthlyCharges > median).        df: Feature dataframe with MonthlyCharges and Contract columns.    Args:    """Assign retention discount treatment with higher probability for high-risk segments.) -> pd.Series:    random_state: int = 42,    treatment_prob_baseline: float = 0.30,    treatment_prob_month_to_month: float = 0.70,    treatment_prob_high_charge: float = 0.75,    df: pd.DataFrame,def assign_synthetic_treatment(from src.model import load_featured_framefrom sklearn.model_selection import train_test_splitfrom sklearn.linear_model import LinearRegressionfrom sklearn.ensemble import RandomForestClassifier, RandomForestRegressorfrom econml.heterogeneous_model_final import RegressionRFimport pandas as pdimport numpy as npfrom typing import Anyfrom pathlib import Pathfrom __future__ import annotationsmatplotlib>=3.8
seaborn>=0.13
scikit-learn>=1.4
joblib>=1.3
jupyter>=1.0
xgboost>=2.0
imbalanced-learn>=0.12
shap>=0.42
dash>=2.13
plotly>=5.14
gunicorn>=21.0